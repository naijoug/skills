#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR/apps"

export VITE_SKILLS_MANAGER_API_URL="${VITE_SKILLS_MANAGER_API_URL:-http://127.0.0.1:8787}"
exec pnpm --filter skills-manager-web dev --host 127.0.0.1
