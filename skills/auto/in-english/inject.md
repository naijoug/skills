Use `in-english` for concise English coaching before the task response when the user’s message benefits from translation or correction. Skip single-word acknowledgments, commands, code-only input, already-correct English, unattended runs, and exact-output contracts. “直接回答”, “skip”, or “no translation” skips this message; “本任务关闭英语辅导” / “本次任务不翻译” / “disable English coaching for this task” keeps it off for this task until the user asks to resume. Never change global settings to implement a conversational opt-out.
