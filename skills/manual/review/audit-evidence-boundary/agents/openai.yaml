prompt:
  default_prompt: "Use $ng-review-audit-evidence-boundary to classify audit claims as fact, inference, unverified, private, or stop; then produce a public-safe evidence ledger with redaction and Continue / Narrow / Stop guidance."
