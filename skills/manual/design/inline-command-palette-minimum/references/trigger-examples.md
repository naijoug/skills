# Routing examples

These are candidate-selection fixtures, not authorization to execute their actions.

## Positive (Chinese)

- "在已有搜索框中加入少量真实命令，不新增全屏弹层，先列出现有 action 对应关系。"
- "给这个产品现有搜索入口设计最小命令集，写清每个动作的可用状态。"

## Positive (English)

- "Add a minimal command experience to the existing search field, using the app's real actions."
- "Design the first few inline commands for this search box and map them to existing handlers and disabled states."

## Negative / Near Miss

- "把搜索按钮的颜色改成蓝色。" <!-- eval: {"route":"none"} -->
- "从零实现一个带复杂参数的独立命令控制台。"
